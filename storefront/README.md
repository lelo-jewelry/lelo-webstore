# lelo-webstore storefront

A Next.js based storefront for [lelo](https://lelo.jewelry).

## Local Development

Copy [.env.template](./.env.template) to `.env` and fill in the required values.

```bash
source .env

npm install
npm run build
npm start
```
